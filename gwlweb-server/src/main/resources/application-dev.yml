server:
  port: 8080

spring:
  application:
    name: du-consumer
  datasource:
    url: jdbc:mysql://localhost:3306/gwlweb?useSSL=false&serverTimezone=Asia/Tokyo&characterEncoding=utf-8
    username: root
    password: 123456

springdoc:
  swagger-ui:
    persistAuthorization: true

dubbo:
  application:
    name: dubbo-consumer
  registry:
    address: zookeeper://3.112.54.245:2181

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true
